---
interface Props {
  heading?: string;
  as?: 'section' | 'div';
}

const { heading, as: Tag = 'section' } = Astro.props;
---

<Tag class="card">
  {heading && <h2>{heading}</h2>}
  <slot />
</Tag>
